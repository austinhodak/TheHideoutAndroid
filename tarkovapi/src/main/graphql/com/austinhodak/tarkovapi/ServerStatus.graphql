query ServerStatus {
    status {
        generalStatus {
            name
            message
            status
        }
        currentStatuses {
            name
            message
            status
        }
        messages {
            content
            time
            type
            solveTime
        }
    }
}